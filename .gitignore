node_modules
# superpowers brainstorming mockups
.superpowers/
# KB corpus (generated)
拆书_文本/
.kbvenv/
__pycache__/
# pnpm-only repo: never commit npm lockfiles (agent-ui/server 都用 pnpm)
package-lock.json
# macOS Finder metadata
.DS_Store
# Playwright MCP 缓存
.playwright-mcp/
.env
# 部署打包产物:只 ignore 镜像 tar(本地生成,几百 MB 不入 git)。
# dist 里的文本文件(docker-compose.yml / .env.example / NAS-DEPLOY.md / Caddyfile)必须入库 ——
# 它们是 NAS load 模式的源(load 模式 ≠ 根的 build 模式,清了 dist 不能从根恢复)。
dist/*.tar.gz
