# agent-ui 构建期环境变量示例。
# 此文件不会被读入运行时 —— NEXT_PUBLIC_* 是 `next build` 时静态 inline 进前端 bundle。
# 实际值通过 docker compose 的 build args 注入(见仓库根 .env.example)。

# 部署后 server 的入口 URL,作为前端 selectedEndpoint 的默认值
# (浏览器 localStorage 持久化值优先,所以老用户不受影响)。
#
# Compose 路(推荐):设成 Caddy 同源地址 ——
#   本地  → http://localhost
#   prod  → https://<域名>
# 浏览器请求 <此值>/auth/login 等即同源,Caddy 按前缀反代到内网 server,无 CORS。
#
# 不设 → 回退 http://localhost:3001(仅适合本地 dev,不是部署态)。
NEXT_PUBLIC_DEFAULT_ENDPOINT=http://localhost:3001
