# agent-ui 构建期环境变量示例(可选)。
# NEXT_PUBLIC_* 是 `next build` 时静态 inline 进前端 bundle;此文件不会被读入运行时。
#
# 默认【不设】—— store.ts 的 selectedEndpoint 回退到 window.location.origin:
#   浏览器打开 http://localhost → 默认连 localhost
#   浏览器打开 https://<域名>  → 默认连该域名
# Caddy 同源反代到内网 server,部署后零配置、无 CORS。
#
# 仅【跨源部署】才显式设值 —— 即 agent-ui 与 server 不在同源(例如前端单独托管到别的域名):
#   NEXT_PUBLIC_DEFAULT_ENDPOINT=https://api.narratox.example.com
# (server 的 CORS 已开 origin:true + credentials:true,跨源直通。)
